---
phase: 06-schema-structured-data
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/pages/servicii/website.astro
  - src/pages/servicii/aplicatii-web.astro
  - src/pages/servicii/seo.astro
  - src/pages/servicii/accesibilitate.astro
  - src/pages/servicii/implementare-agenti.astro
  - src/pages/en/services/website.astro
  - src/pages/en/services/web-apps.astro
  - src/pages/en/services/seo.astro
  - src/pages/en/services/accessibility.astro
  - src/pages/en/services/ai-agents.astro
  - src/pages/despre.astro
  - src/pages/en/about.astro
  - src/pages/servicii/index.astro
  - src/pages/en/services/index.astro
  - src/pages/portofoliu/index.astro
  - src/pages/en/portfolio/index.astro
  - src/pages/pret.astro
  - src/pages/en/pricing.astro
  - src/pages/contact.astro
  - src/pages/en/contact.astro
autonomous: true
requirements: [SCHEMA-01-prop-fix, SCHEMA-02-graph-rewrite, SCHEMA-03-breadcrumb]

must_haves:
  truths:
    - "Service pages pass pageType='service' and serviceName to Page.astro"
    - "About pages pass pageType='about' to Page.astro"
    - "All non-homepage pages receive correct pageType for schema generation"
    - "Build completes with zero errors and all schemas render in HTML output"
  artifacts:
    - path: "src/pages/servicii/website.astro"
      provides: "Service page with pageType and serviceName props"
      contains: "pageType"
    - path: "src/pages/despre.astro"
      provides: "About page with pageType='about'"
      contains: "pageType"
  key_links:
    - from: "src/pages/servicii/*.astro"
      to: "src/layouts/Page.astro"
      via: "pageType='service' serviceName={title}"
      pattern: "pageType.*service"
    - from: "src/pages/despre.astro"
      to: "src/layouts/Page.astro"
      via: "pageType='about'"
      pattern: "pageType.*about"
---

<objective>
Update all page files to pass the correct pageType and serviceName props to Page.astro, then verify schemas render in build output.

Purpose: Plan 01 fixed the prop-passing infrastructure and rewrote JsonLd.astro. Now every page needs to declare its type so the correct schemas render. Service pages need pageType="service" + serviceName. About pages need pageType="about". Other pages need their respective types for proper BreadcrumbList generation.

Output: All 20 page files updated with correct props. Build verified with schemas present in HTML output.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schema-structured-data/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Page.astro interface (fixed in Plan 01): -->
```typescript
interface Props {
  title: string;
  description?: string;
  locale: Locale;
  roSlug: string;
  enSlug: string;
  ogImage?: string;
  pageType?: 'home' | 'service' | 'pricing' | 'portfolio' | 'about' | 'contact' | 'default';
  serviceName?: string;
}
```

<!-- Service page pattern (current, from src/pages/servicii/website.astro): -->
```astro
<Page title={title} description={description} locale={locale} roSlug={roSlug} enSlug={enSlug}>
  <ServicePageLayout serviceKey={serviceKey} locale={locale} />
</Page>
```

<!-- About page pattern (current, from src/pages/despre.astro): -->
```astro
<Page title="Despre — Isio" description="..." locale={locale} roSlug={roSlug} enSlug={enSlug}>
```

<!-- Service titles from content JSON (used for serviceName prop): -->
<!-- web-development: { ro: "Dezvoltare Web", en: "Web Development" } -->
<!-- web-apps: { ro: "Aplicatii Web", en: "Web Apps" } -->
<!-- seo: { ro: "SEO", en: "SEO" } -->
<!-- accessibility: { ro: "Accesibilitate Web", en: "Web Accessibility" } -->
<!-- ai-agents: { ro: "Agenti AI", en: "AI Agents" } -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add pageType and serviceName props to all page files</name>
  <files>
src/pages/servicii/website.astro
src/pages/servicii/aplicatii-web.astro
src/pages/servicii/seo.astro
src/pages/servicii/accesibilitate.astro
src/pages/servicii/implementare-agenti.astro
src/pages/en/services/website.astro
src/pages/en/services/web-apps.astro
src/pages/en/services/seo.astro
src/pages/en/services/accessibility.astro
src/pages/en/services/ai-agents.astro
src/pages/despre.astro
src/pages/en/about.astro
src/pages/servicii/index.astro
src/pages/en/services/index.astro
src/pages/portofoliu/index.astro
src/pages/en/portfolio/index.astro
src/pages/pret.astro
src/pages/en/pricing.astro
src/pages/contact.astro
src/pages/en/contact.astro
  </files>
  <read_first>
src/pages/servicii/website.astro
src/pages/servicii/aplicatii-web.astro
src/pages/servicii/seo.astro
src/pages/servicii/accesibilitate.astro
src/pages/servicii/implementare-agenti.astro
src/pages/en/services/website.astro
src/pages/en/services/web-apps.astro
src/pages/en/services/seo.astro
src/pages/en/services/accessibility.astro
src/pages/en/services/ai-agents.astro
src/pages/despre.astro
src/pages/en/about.astro
src/pages/servicii/index.astro
src/pages/en/services/index.astro
src/pages/portofoliu/index.astro
src/pages/en/portfolio/index.astro
src/pages/pret.astro
src/pages/en/pricing.astro
src/pages/contact.astro
src/pages/en/contact.astro
  </read_first>
  <action>
For each file, update the `<Page>` component call to include the appropriate `pageType` prop. For service pages, also add `serviceName`. The `title` variable is already available in each file from either `localize(service.metaTitle, locale)` or a hardcoded string.

**Service pages (10 files) — add `pageType="service"` and `serviceName={title}`:**

Each service page already has a `title` variable derived from the content JSON. Use that as the serviceName since it's the localized service name.

Change pattern from:
```html
<Page title={title} description={description} locale={locale} roSlug={roSlug} enSlug={enSlug}>
```
To:
```html
<Page title={title} description={description} locale={locale} roSlug={roSlug} enSlug={enSlug} pageType="service" serviceName={title}>
```

Apply to these 10 files:
- `src/pages/servicii/website.astro` — serviceName={title} (resolves to "Dezvoltare Web — Isio")
- `src/pages/servicii/aplicatii-web.astro` — serviceName={title}
- `src/pages/servicii/seo.astro` — serviceName={title}
- `src/pages/servicii/accesibilitate.astro` — serviceName={title}
- `src/pages/servicii/implementare-agenti.astro` — serviceName={title}
- `src/pages/en/services/website.astro` — serviceName={title}
- `src/pages/en/services/web-apps.astro` — serviceName={title}
- `src/pages/en/services/seo.astro` — serviceName={title}
- `src/pages/en/services/accessibility.astro` — serviceName={title}
- `src/pages/en/services/ai-agents.astro` — serviceName={title}

NOTE: The `title` variable includes " — Isio" suffix (e.g., "Dezvoltare Web — Isio"). For cleaner serviceName, extract just the service name portion. Add this line before the Page call in each service page:
```typescript
const serviceLabel = title.split(' — ')[0];
```
Then use `serviceName={serviceLabel}` instead of `serviceName={title}`.

**About pages (2 files) — add `pageType="about"`:**

- `src/pages/despre.astro`: Change `<Page title="Despre — Isio"` to `<Page title="Despre — Isio" ... pageType="about">`
- `src/pages/en/about.astro`: Change `<Page title="About — Isio"` to `<Page title="About — Isio" ... pageType="about">`

**Services index pages (2 files) — add `pageType="default"`:**

- `src/pages/servicii/index.astro`: Add `pageType="default"` (services listing, not a specific service)
- `src/pages/en/services/index.astro`: Add `pageType="default"`

**Portfolio pages (2 files) — add `pageType="portfolio"`:**

- `src/pages/portofoliu/index.astro`: Add `pageType="portfolio"`
- `src/pages/en/portfolio/index.astro`: Add `pageType="portfolio"`

**Pricing pages (2 files) — add `pageType="pricing"`:**

- `src/pages/pret.astro`: Add `pageType="pricing"`
- `src/pages/en/pricing.astro`: Add `pageType="pricing"`

**Contact pages (2 files) — add `pageType="contact"`:**

- `src/pages/contact.astro`: Add `pageType="contact"`
- `src/pages/en/contact.astro`: Add `pageType="contact"`

**Homepage (already done):** `src/pages/index.astro` and `src/pages/en/index.astro` already pass `pageType="home"`. No changes needed.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && grep -l "pageType" src/pages/servicii/website.astro src/pages/servicii/seo.astro src/pages/despre.astro src/pages/en/about.astro src/pages/en/services/website.astro src/pages/contact.astro src/pages/en/contact.astro src/pages/pret.astro src/pages/en/pricing.astro src/pages/portofoliu/index.astro && grep "serviceName" src/pages/servicii/website.astro src/pages/en/services/website.astro</automated>
  </verify>
  <acceptance_criteria>
- `grep "pageType" src/pages/servicii/website.astro` returns match containing `pageType="service"`
- `grep "serviceName" src/pages/servicii/website.astro` returns match
- `grep "pageType" src/pages/despre.astro` returns match containing `pageType="about"`
- `grep "pageType" src/pages/en/about.astro` returns match containing `pageType="about"`
- `grep "pageType" src/pages/contact.astro` returns match containing `pageType="contact"`
- `grep "pageType" src/pages/pret.astro` returns match containing `pageType="pricing"`
- `grep "pageType" src/pages/portofoliu/index.astro` returns match containing `pageType="portfolio"`
- All 10 service pages have both `pageType="service"` and `serviceName=`
- All 20 files have a `pageType` prop on their `<Page>` call
  </acceptance_criteria>
  <done>All 20 page files pass correct pageType to Page.astro. Service pages also pass serviceName.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification — confirm schemas render in HTML output</name>
  <files></files>
  <read_first>src/components/seo/JsonLd.astro</read_first>
  <action>
Run `npm run build` and verify schema output in the generated HTML files.

1. Run `npm run build` — must complete with zero errors.

2. Check homepage HTML for FAQPage schema:
```bash
grep -o "FAQPage" dist/index.html
```
Must find "FAQPage" in the output.

3. Check homepage HTML for @graph pattern:
```bash
grep -o "@graph" dist/index.html
```
Must find "@graph".

4. Check a service page for Service schema:
```bash
grep -o '"Service"' dist/servicii/website/index.html
```
Must find "Service".

5. Check about page for Person schema (founder):
```bash
grep -o '"Person"' dist/despre/index.html
```
Must find "Person".

6. Check an inner page for BreadcrumbList:
```bash
grep -o "BreadcrumbList" dist/despre/index.html
```
Must find "BreadcrumbList".

7. Check that ProfessionalService has email:
```bash
grep -o "contact@isio.ro" dist/index.html
```
Must find "contact@isio.ro".

8. Check logo is raster:
```bash
grep -o "og.png" dist/index.html
```
Must find "og.png" (not "favicon.svg").

9. Verify no old SVG logo reference:
```bash
grep "favicon.svg" dist/index.html | grep -v 'rel="icon"'
```
Must return nothing (favicon.svg only in the link icon tag, not in schema).

If any check fails, diagnose and fix the issue in the relevant source file.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && npm run build 2>&1 | tail -5 && grep -c "@graph" dist/index.html && grep -c "FAQPage" dist/index.html && grep -c "BreadcrumbList" dist/despre/index.html && grep -c '"Service"' dist/servicii/website/index.html && grep -c '"Person"' dist/despre/index.html && grep -c "contact@isio.ro" dist/index.html && grep -c "og.png" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
- `npm run build` exits with code 0
- `grep "@graph" dist/index.html` returns at least 1 match
- `grep "FAQPage" dist/index.html` returns at least 1 match
- `grep "BreadcrumbList" dist/despre/index.html` returns at least 1 match
- `grep '"Service"' dist/servicii/website/index.html` returns at least 1 match
- `grep '"Person"' dist/despre/index.html` returns at least 1 match
- `grep "contact@isio.ro" dist/index.html` returns at least 1 match
- `grep "og.png" dist/index.html` returns at least 1 match (raster logo)
- `grep "favicon.svg" dist/index.html | grep -v 'rel="icon"'` returns empty (no SVG in schema)
  </acceptance_criteria>
  <done>Build passes. All schema types verified present in HTML output: @graph pattern, FAQPage on homepage, BreadcrumbList on inner pages, Service on service pages, Person on about page, complete ProfessionalService with email/sameAs/founder/logo.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` — exits 0
2. Homepage: @graph, FAQPage, ProfessionalService (email, sameAs, founder, foundingDate, og.png logo)
3. Service pages: Service schema with provider @id reference
4. About page: Person schema with founder details
5. Inner pages: BreadcrumbList with correct crumb chain
6. No more separate @context blocks — single unified @graph
</verification>

<success_criteria>
- All 20 page files pass correct pageType (and serviceName where applicable)
- Build completes without errors
- HTML output contains correct schema for every page type
- FAQPage renders on homepage only
- BreadcrumbList renders on all inner pages
- Person schema renders on about pages
- Service schema renders on service pages
- ProfessionalService has email, sameAs, founder, foundingDate, raster logo
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-structured-data/06-02-SUMMARY.md`
</output>
