---
phase: 07-content-cleanup-trust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/ContactInline.astro
  - src/components/home/Hero.astro
  - src/components/layout/MobileMenu.astro
  - src/components/layout/Footer.astro
  - public/og.png
autonomous: true
requirements:
  - TRUST-05
  - TRUST-06
  - TRUST-07
  - TRUST-08

must_haves:
  truths:
    - "Social links in contact section point to real URLs (LinkedIn) or are removed (GitHub)"
    - "Entity name is consistently Isio in all alt attributes and visible text"
    - "OG image exists at /og.png and is a valid PNG file"
    - "Footer displays city (Iasi) and founding year (2025)"
  artifacts:
    - path: "src/components/home/ContactInline.astro"
      provides: "Real social links, no href=#"
      contains: "linkedin.com"
    - path: "src/components/home/Hero.astro"
      provides: "Consistent alt=Isio"
      contains: 'alt="Isio"'
    - path: "src/components/layout/MobileMenu.astro"
      provides: "Consistent alt=Isio"
      contains: 'alt="Isio"'
    - path: "src/components/layout/Footer.astro"
      provides: "Legal info with city and founding year"
      contains: "Iași"
    - path: "public/og.png"
      provides: "OG image for social sharing"
      min_lines: 0
  key_links:
    - from: "src/layouts/Base.astro"
      to: "public/og.png"
      via: "OG meta tag references /og.png"
      pattern: "og\\.png"
---

<objective>
Fix trust signals: replace placeholder social links with real URLs, standardize entity name to "Isio" everywhere, create OG image for social sharing, and add legal entity info (city, founding year) to the footer.

Purpose: Improve E-E-A-T trustworthiness by removing broken links and adding verifiable business information.
Output: Consistent branding, working social links, OG image, legal footer info.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key component interfaces the executor needs -->

From src/components/home/ContactInline.astro:
- Lines 31-36: Two social link `<a>` tags with `href="#"` — GitHub and LinkedIn icons
- GitHub: `<a href="#" aria-label="GitHub"...>`
- LinkedIn: `<a href="#" aria-label="LinkedIn"...>`

From src/components/home/Hero.astro:
- Line 132: `<img src="/images/logo.svg" alt="ISIO"...>` — needs alt="Isio"

From src/components/layout/MobileMenu.astro:
- Line 30: `<img src="/images/logo.svg" alt="ISIO"...>` — needs alt="Isio"

From src/components/layout/Footer.astro:
- Lines 13, 17: copyright text with "Est. 2020" — needs to be "Est. 2025" and add city
- Currently no city info beyond the copyright line

From src/layouts/Base.astro:
- Line 20: `const defaultOgImage = '${baseUrl}/og.png';` — already references og.png
- OG meta tags already configured, just need the actual file to exist

Real profile URLs per CONTEXT.md:
- LinkedIn: https://www.linkedin.com/in/silviupaduraru/
- GitHub: https://github.com/SilviuPad (from memory context)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix social links and standardize entity name</name>
  <files>src/components/home/ContactInline.astro, src/components/home/Hero.astro, src/components/layout/MobileMenu.astro</files>
  <action>
**ContactInline.astro — Fix placeholder social links (audit #21):**

1. Replace the GitHub link `href="#"` with `href="https://github.com/SilviuPad"` and add `target="_blank" rel="noopener noreferrer"`.

2. Replace the LinkedIn link `href="#"` with `href="https://www.linkedin.com/in/silviupaduraru/"` and add `target="_blank" rel="noopener noreferrer"`.

The two `<a>` elements at lines 31-36 should become:
```html
<a href="https://github.com/SilviuPad" target="_blank" rel="noopener noreferrer" aria-label="GitHub" class="w-10 h-10 rounded-full border border-white/10 flex items-center justify-center text-base-300 hover:text-electric-400 hover:border-electric-400/50 transition-all">
```
```html
<a href="https://www.linkedin.com/in/silviupaduraru/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn" class="w-10 h-10 rounded-full border border-white/10 flex items-center justify-center text-base-300 hover:text-electric-400 hover:border-electric-400/50 transition-all">
```

**Hero.astro — Fix entity name (audit #27):**

Change line 132 from `alt="ISIO"` to `alt="Isio"`.

**MobileMenu.astro — Fix entity name (audit #27):**

Change line 30 from `alt="ISIO"` to `alt="Isio"`.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && grep 'href="#"' src/components/home/ContactInline.astro && echo "FAIL: placeholder links remain" || echo "PASS: no placeholder links" && grep 'alt="ISIO"' src/components/home/Hero.astro src/components/layout/MobileMenu.astro && echo "FAIL: ISIO still in alt" || echo "PASS: entity name fixed"</automated>
  </verify>
  <done>Social links point to real GitHub and LinkedIn profiles. All img alt attributes use "Isio" not "ISIO".</done>
</task>

<task type="auto">
  <name>Task 2: Create OG image and add legal info to footer</name>
  <files>public/og.png, src/components/layout/Footer.astro</files>
  <action>
**OG Image (audit #11):**

Create a simple 1200x630 OG image at `public/og.png`. Since we cannot generate raster images programmatically in this context, create the OG image using one of these approaches (in order of preference):

Option A (preferred): Use Node.js with a canvas library if available. If `sharp` or `@napi-rs/canvas` is in node_modules, create a script to generate a simple 1200x630 dark-background PNG with "Isio" text centered.

Option B: Create a minimal valid 1200x630 PNG using raw binary data. Create a simple Node.js script at `scripts/generate-og.ts` that:
```typescript
import { writeFileSync } from 'fs';
// Generate a minimal 1200x630 PNG with dark background (#0a0a0f) 
// Use a simple approach: create an HTML file and note that the user should screenshot it
// OR use the `sharp` library if available
```

Option C (fallback): Create a simple SVG file at `public/og.svg` with dimensions 1200x630, dark background (#0a0a0f), centered "Isio" text in white, and a subtle gradient line below. Then check if a tool is available to convert it. If no conversion tool exists, create the SVG AND add a `checkpoint:human-action` note in the summary that the user needs to convert it to PNG or provide a real OG image.

**The simplest reliable approach:** Create a tiny Node.js script that generates a minimal valid PNG file using raw buffer manipulation (PNG header + IHDR + IDAT with compressed solid-color data + IEND). A solid dark-colored 1200x630 PNG is fine as a placeholder — it's better than a missing file returning 404. The user can replace it with a branded version later.

Actually, check first: `ls node_modules/sharp 2>/dev/null` — if sharp exists, use it to generate a proper branded PNG.

If no image generation tools available, create a 1x1 placeholder PNG (valid file) and leave a comment in the summary that the user should replace it with a proper 1200x630 branded image.

**Footer.astro — Add legal info (audit #31):**

Update the footer to include city (Iasi) and correct founding year (2025). The current text is:
- RO: `(c) 2026 Isio. Est. 2020. Toate drepturile rezervate.`
- EN: `(c) 2026 Isio. Est. 2020. All rights reserved.`

Change to:
- RO: `(c) 2026 Isio. Iași, Romania. Est. 2025. Toate drepturile rezervate.`
- EN: `(c) 2026 Isio. Iași, Romania. Est. 2025. All rights reserved.`

Update the `footerText` record in the frontmatter script section:
```typescript
ro: {
  copyright: `\u00A9 ${currentYear} Isio. Ia\u0219i, Romania. Est. 2025. Toate drepturile rezervate.`,
  privacy: 'Politica de confiden\u021Bialitate',
},
en: {
  copyright: `\u00A9 ${currentYear} Isio. Ia\u0219i, Romania. Est. 2025. All rights reserved.`,
  privacy: 'Privacy Policy',
},
```

Note: The `\u0219` is the Unicode escape for `ș` (s-comma-below, the correct Romanian character for Iasi).
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && test -f public/og.png && echo "PASS: og.png exists" || echo "FAIL: og.png missing" && grep "2025" src/components/layout/Footer.astro > /dev/null && echo "PASS: founding year 2025" || echo "FAIL" && grep -i "ia" src/components/layout/Footer.astro > /dev/null && echo "PASS: city present" || echo "FAIL"</automated>
  </verify>
  <done>OG image file exists at public/og.png (valid PNG). Footer shows "Iasi, Romania. Est. 2025" in both locales. The "Est. 2020" error is corrected to 2025.</done>
</task>

</tasks>

<verification>
- `grep 'href="#"' src/components/home/ContactInline.astro` returns no matches
- `grep 'alt="ISIO"' src/components/home/Hero.astro src/components/layout/MobileMenu.astro` returns no matches
- `test -f public/og.png` passes
- `grep "Est. 2025" src/components/layout/Footer.astro` returns matches
- `grep "Ia" src/components/layout/Footer.astro` returns matches (city present)
- Build succeeds: `npm run build`
</verification>

<success_criteria>
- No `href="#"` social links anywhere in ContactInline.astro
- LinkedIn link points to https://www.linkedin.com/in/silviupaduraru/
- GitHub link points to https://github.com/SilviuPad
- All `alt="ISIO"` changed to `alt="Isio"` across Hero.astro and MobileMenu.astro
- public/og.png exists as a valid PNG file
- Footer shows city (Iasi) and founding year (2025) in both RO and EN
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-cleanup-trust/07-02-SUMMARY.md`
</output>
