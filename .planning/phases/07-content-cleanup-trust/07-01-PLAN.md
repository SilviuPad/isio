---
phase: 07-content-cleanup-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/components/home/BlogPreview.astro
  - src/components/home/Stats.astro
  - src/components/home/Partners.astro
  - public/images/blog/blog1.svg
  - public/images/blog/blog2.svg
  - public/images/blog/blog3.svg
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - TRUST-04

must_haves:
  truths:
    - "No blog section renders on either homepage (RO or EN)"
    - "Stats counters show real numeric values in raw HTML source (not 0+)"
    - "Projects stat shows 6, not 50"
    - "Partners section heading says Technologies We Use / Tehnologii, not Partners"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage without BlogPreview import or usage"
      contains: "no BlogPreview"
    - path: "src/pages/en/index.astro"
      provides: "EN homepage without BlogPreview import or usage"
      contains: "no BlogPreview"
    - path: "src/components/home/Stats.astro"
      provides: "Stats with real values in HTML and visual-only animation"
      contains: "data-counter"
    - path: "src/components/home/Partners.astro"
      provides: "Renamed section heading"
      contains: "Tehnologii"
  key_links:
    - from: "src/components/home/Stats.astro"
      to: "HTML output"
      via: "Server-rendered text content matches data-counter values"
      pattern: ">6\\+<|>7\\+<|>100%<"
---

<objective>
Remove fake/misleading content from homepage: delete the fabricated blog section, fix stats counters to render real values in HTML (crawlers currently see "0+"), correct the "50+ projects" claim to match the actual 6 portfolio items, and rename the misleading "Partners & Technologies" section to "Technologies We Use."

Purpose: Eliminate E-E-A-T trust damage from fabricated content and unsubstantiated claims.
Output: Clean homepage with honest stats, no fake blog, accurately labeled tech section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SEO-AUDIT.md (sections 2.1, 2.6 for trust signal issues)

<interfaces>
<!-- Key component interfaces the executor needs -->

From src/pages/index.astro (RO homepage):
- Imports BlogPreview at line 14, uses at line 41 — REMOVE both
- Imports Partners at line 15, uses at line 42 — KEEP (just rename heading inside Partners.astro)

From src/pages/en/index.astro (EN homepage):
- Imports BlogPreview at line 14, uses at line 41 — REMOVE both

From src/components/home/Stats.astro:
- Three counters with data-counter="50" (projects), data-counter="7" (experience), data-counter="100" (satisfaction)
- All render initial text content "0+" or "0%" — crawlers see these zeros
- GSAP ScrollTrigger animates from 0 to target on scroll

From src/components/home/Partners.astro:
- Section tag: '[ 09 ] parteneri & tehnologii' (RO) / '[ 09 ] partners & technologies' (EN)
- H2: 'Colaboratorii nostri de incredere...' (RO) / 'Our trusted collaborators...' (EN)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fake blog section and delete blog assets</name>
  <files>src/pages/index.astro, src/pages/en/index.astro, src/components/home/BlogPreview.astro, public/images/blog/blog1.svg, public/images/blog/blog2.svg, public/images/blog/blog3.svg</files>
  <action>
1. In `src/pages/index.astro`:
   - Delete the import line: `import BlogPreview from '../components/home/BlogPreview.astro';`
   - Delete the usage line: `<BlogPreview locale={locale} />`

2. In `src/pages/en/index.astro`:
   - Delete the import line: `import BlogPreview from '../../components/home/BlogPreview.astro';`
   - Delete the usage line: `<BlogPreview locale={locale} />`

3. Delete the component file: `src/components/home/BlogPreview.astro`

4. Delete the blog placeholder images:
   - `public/images/blog/blog1.svg`
   - `public/images/blog/blog2.svg`
   - `public/images/blog/blog3.svg`

5. Check if `public/images/blog/` directory is now empty. If so, delete the directory.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && grep -r "BlogPreview" src/pages/ && echo "FAIL: BlogPreview still referenced" || echo "PASS: BlogPreview removed" && test ! -f src/components/home/BlogPreview.astro && echo "PASS: component deleted" || echo "FAIL: component still exists"</automated>
  </verify>
  <done>No BlogPreview import or usage in either homepage file, component file deleted, blog SVGs deleted</done>
</task>

<task type="auto">
  <name>Task 2: Fix stats counters and rename Partners section</name>
  <files>src/components/home/Stats.astro, src/components/home/Partners.astro</files>
  <action>
**Stats.astro — Fix counter values (audit #23 and #13):**

The problem: counters render "0+" as initial HTML text, so crawlers/AI see zero values. The "50+" projects claim is also misleading (only 6 portfolio items exist).

1. Change the projects counter from `data-counter="50"` to `data-counter="6"`. Change the initial text content from `0+` to `6+`. This means the HTML source shows the real value. The label stays "Proiecte livrate" / "Projects delivered".

2. Change the experience counter initial text from `0+` to `7+`. Keep `data-counter="7"`.

3. Change the satisfaction counter initial text from `0%` to `100%`. Keep `data-counter="100"`.

4. Update the `<script>` animation so it still provides a visual counting effect but starts from the real value if JS is disabled. The animation should:
   - Read the target from `data-counter` and suffix from `data-suffix`
   - Animate from 0 to target visually (same GSAP ScrollTrigger behavior)
   - The HTML source already contains the correct final value as the element's text content

So the three div elements become:
```
<div ... data-counter="6" data-suffix="+">6+</div>
<div ... data-counter="7" data-suffix="+">7+</div>
<div ... data-counter="100" data-suffix="%">100%</div>
```

5. In the script section, add a line at the start of the `onEnter` callback to set the element text to "0" + suffix before animating (so the visual animation still counts up from 0, but the no-JS fallback shows the real value):
```js
onEnter: () => {
  if (started) return;
  started = true;
  el.textContent = '0' + suffix; // Reset for animation
  const duration = 4000;
  // ... rest of animation unchanged
```

**Partners.astro — Rename section (audit #20):**

1. Change the section tag text:
   - RO: `'[ 09 ] parteneri & tehnologii'` to `'[ 09 ] tehnologii'`
   - EN: `'[ 09 ] partners & technologies'` to `'[ 09 ] technologies'`

2. Change the h2 text:
   - RO: `'Colaboratorii nostri de incredere in progres si succes'` to `'Tehnologiile pe care le folosim'`
   - EN: `'Our trusted collaborators in progress and success'` to `'Technologies We Use'`
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && grep -c 'data-counter="6"' src/components/home/Stats.astro | grep -q "1" && echo "PASS: projects counter=6" || echo "FAIL" && grep ">6+<\|6+" src/components/home/Stats.astro > /dev/null && echo "PASS: HTML shows 6+" || echo "FAIL: HTML still shows 0+" && grep "Tehnologii" src/components/home/Partners.astro > /dev/null && echo "PASS: Partners renamed" || echo "FAIL"</automated>
  </verify>
  <done>Stats counters show real values (6+, 7+, 100%) in HTML source with visual-only animation. Partners section renamed to "Tehnologiile pe care le folosim" / "Technologies We Use".</done>
</task>

</tasks>

<verification>
- `grep -r "BlogPreview" src/` returns no matches
- `test ! -f src/components/home/BlogPreview.astro` passes
- `grep "0+" src/components/home/Stats.astro` returns no matches (all initial values are real)
- `grep 'data-counter="50"' src/components/home/Stats.astro` returns no matches
- `grep -i "partner" src/components/home/Partners.astro` returns no matches in user-facing text
- Build succeeds: `npm run build`
</verification>

<success_criteria>
- BlogPreview component fully removed from codebase (import, usage, file, assets)
- Stats HTML source contains "6+", "7+", "100%" (not "0+", "0%")
- Projects stat uses data-counter="6" (matches real portfolio count)
- Partners section heading says "Tehnologiile pe care le folosim" / "Technologies We Use"
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-cleanup-trust/07-01-SUMMARY.md`
</output>
