---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/env.d.ts
  - src/i18n/messages/ro.json
  - src/i18n/messages/en.json
  - src/i18n/utils.ts
  - project.inlang/settings.json
  - .env.example
  - .gitignore
autonomous: true
requirements: [I18N-01, I18N-03, SEO-06]

must_haves:
  truths:
    - "Astro project builds successfully with zero errors"
    - "i18n routing configured with RO as default (no prefix) and EN under /en/"
    - "Paraglide i18n messages compile to type-safe functions"
    - "Tailwind CSS v4 processes utility classes"
    - "Cloudflare Workers adapter is configured for hybrid server/static output"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro configuration with i18n, Cloudflare, Tailwind, sitemap"
      contains: "prefixDefaultLocale: false"
    - path: "src/i18n/utils.ts"
      provides: "Locale utility functions"
      exports: ["getLocaleFromUrl", "localePath", "getAlternateLocaleSlug"]
    - path: "src/i18n/messages/ro.json"
      provides: "Romanian UI chrome translations"
      contains: "nav.services"
    - path: "src/i18n/messages/en.json"
      provides: "English UI chrome translations"
      contains: "nav.services"
    - path: "project.inlang/settings.json"
      provides: "Paraglide JS language configuration"
      contains: "sourceLanguageTag"
  key_links:
    - from: "astro.config.mjs"
      to: "@astrojs/cloudflare"
      via: "adapter import"
      pattern: "adapter:\\s*cloudflare"
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite plugins"
      pattern: "tailwindcss\\(\\)"
    - from: "astro.config.mjs"
      to: "@inlang/paraglide-js"
      via: "vite plugins"
      pattern: "paraglideVitePlugin"
---

<objective>
Initialize the Astro 6 project with all core dependencies, i18n routing configuration, Paraglide JS message catalog, Tailwind CSS v4, and Cloudflare Workers adapter.

Purpose: Establish the buildable project skeleton that all other plans depend on — routing, styling, i18n, and deployment adapter.
Output: A working Astro project that builds successfully with i18n routing, Tailwind v4, and Cloudflare adapter configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with all dependencies and core configuration</name>
  <files>package.json, astro.config.mjs, tsconfig.json, src/env.d.ts, .env.example, .gitignore</files>
  <read_first>.gitignore, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
Create the Astro project and install all dependencies. Do NOT use `npm create astro@latest` interactively — instead, manually create the project files to avoid interactive prompts.

1. Create `package.json` with these dependencies:
   - dependencies: `astro` (^6.0), `@astrojs/cloudflare` (latest), `@astrojs/sitemap` (latest), `tailwindcss` (^4.2), `@tailwindcss/vite` (^4.2), `@sanity/astro` (^4), `@sanity/client` (^6), `sanity` (^3), `astro-seo` (^1.1.0), `@inlang/paraglide-js` (^2.15.0)
   - devDependencies: `typescript` (^5)
   - scripts: `"dev": "astro dev"`, `"build": "astro build"`, `"preview": "astro preview"`, `"astro": "astro"`
   - `"type": "module"`

2. Create `astro.config.mjs` with EXACTLY this configuration (per D-04, D-07, D-08, research patterns):
```javascript
import { defineConfig } from 'astro/config';
import cloudflare from '@astrojs/cloudflare';
import tailwindcss from '@tailwindcss/vite';
import sitemap from '@astrojs/sitemap';
import sanity from '@sanity/astro';
import { paraglideVitePlugin } from '@inlang/paraglide-js';

export default defineConfig({
  site: 'https://isio.ro',
  output: 'server',
  adapter: cloudflare(),
  i18n: {
    locales: ['ro', 'en'],
    defaultLocale: 'ro',
    routing: {
      prefixDefaultLocale: false,
    },
    fallback: { en: 'ro' },
  },
  integrations: [
    sanity({
      projectId: import.meta.env.SANITY_PROJECT_ID || '',
      dataset: 'production',
      apiVersion: '2025-02-19',
      useCdn: false,
      studioBasePath: '/studio',
    }),
    sitemap({
      i18n: {
        defaultLocale: 'ro',
        locales: { ro: 'ro-RO', en: 'en-US' },
      },
    }),
  ],
  vite: {
    plugins: [
      tailwindcss(),
      paraglideVitePlugin({
        project: './project.inlang',
        outdir: './src/paraglide',
      }),
    ],
  },
});
```

IMPORTANT: Do NOT use `output: 'hybrid'` — it was removed in Astro 6. Use `output: 'server'`.
IMPORTANT: Do NOT use `@astrojs/tailwind` — it is deprecated. Use `@tailwindcss/vite` as a Vite plugin.

3. Create `tsconfig.json`:
```json
{
  "extends": "astro/tsconfigs/strict"
}
```

4. Create `src/env.d.ts`:
```typescript
/// <reference types="astro/client" />
```

5. Create `.env.example` with all required environment variables:
```
SANITY_PROJECT_ID=your-project-id
SANITY_DATASET=production
SANITY_API_READ_TOKEN=your-read-token
```

6. Update `.gitignore` — append Astro-specific entries to existing file:
```
# Astro
.astro/
dist/

# Paraglide generated
src/paraglide/

# Sanity
.sanity/

# Cloudflare
.wrangler/
```

7. Run `npm install` to install all dependencies.

8. Create a minimal `src/pages/index.astro` placeholder so the project builds:
```astro
---
export const prerender = true;
---
<html lang="ro-RO">
  <head><title>Isio</title></head>
  <body><h1>Isio</h1></body>
</html>
```
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && npx astro check 2>&1 | head -20; echo "---"; cat package.json | grep -c "astro"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"astro"` in dependencies
    - package.json contains `"@astrojs/cloudflare"` in dependencies
    - package.json contains `"@tailwindcss/vite"` in dependencies
    - package.json contains `"@sanity/astro"` in dependencies
    - package.json contains `"@inlang/paraglide-js"` in dependencies
    - package.json contains `"type": "module"`
    - astro.config.mjs contains `prefixDefaultLocale: false`
    - astro.config.mjs contains `defaultLocale: 'ro'`
    - astro.config.mjs contains `output: 'server'`
    - astro.config.mjs contains `adapter: cloudflare()`
    - astro.config.mjs contains `tailwindcss()`
    - astro.config.mjs contains `paraglideVitePlugin`
    - astro.config.mjs contains `studioBasePath: '/studio'`
    - tsconfig.json contains `"extends": "astro/tsconfigs/strict"`
    - .env.example contains `SANITY_PROJECT_ID`
    - .gitignore contains `.astro/`
    - node_modules/ directory exists
    - src/pages/index.astro contains `export const prerender = true`
  </acceptance_criteria>
  <done>Astro 6 project initialized with all Phase 1 dependencies installed, astro.config.mjs fully configured with i18n routing (RO default, EN prefixed), Cloudflare Workers adapter, Tailwind v4 Vite plugin, Sanity integration with Studio at /studio, and sitemap with i18n locales. Project builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Set up Paraglide JS i18n messages and locale utility functions</name>
  <files>project.inlang/settings.json, src/i18n/messages/ro.json, src/i18n/messages/en.json, src/i18n/utils.ts</files>
  <read_first>astro.config.mjs, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
1. Create `project.inlang/settings.json` for Paraglide JS v2 configuration:
```json
{
  "$schema": "https://inlang.com/schema/project-settings",
  "sourceLanguageTag": "ro",
  "languageTags": ["ro", "en"],
  "modules": [
    "https://cdn.jsdelivr.net/npm/@inlang/message-lint-rule-empty-pattern@latest/dist/index.js",
    "https://cdn.jsdelivr.net/npm/@inlang/message-lint-rule-missing-translation@latest/dist/index.js",
    "https://cdn.jsdelivr.net/npm/@inlang/plugin-message-format@latest/dist/index.js"
  ],
  "plugin.inlang.messageFormat": {
    "pathPattern": "./src/i18n/messages/{languageTag}.json"
  }
}
```

2. Create `src/i18n/messages/ro.json` with Romanian UI chrome translations:
```json
{
  "nav_services": "Servicii",
  "nav_portfolio": "Portofoliu",
  "nav_pricing": "Prețuri",
  "nav_about": "Despre",
  "nav_contact": "Contact",
  "nav_bookCall": "Programează un apel",
  "lang_switch_ro": "Română",
  "lang_switch_en": "English",
  "footer_copyright": "© {year} Isio. Toate drepturile rezervate.",
  "footer_privacy": "Politica de confidențialitate",
  "meta_defaultDescription": "Isio — agenție web specializată în dezvoltare website-uri, aplicații web, SEO și audituri de accesibilitate.",
  "pricing_basic": "Basic",
  "pricing_standard": "Standard",
  "pricing_premium": "Premium",
  "pricing_recommended": "Recomandat",
  "pricing_deliveryDays": "{days} zile livrare",
  "pricing_revisions": "{count} revizuiri",
  "pricing_revisionsUnlimited": "Revizuiri nelimitate",
  "pricing_startingFrom": "Începând de la",
  "pricing_currency": "EUR",
  "cta_getStarted": "Începe acum",
  "cta_contactUs": "Contactează-ne",
  "error_404_title": "Pagina nu a fost găsită",
  "error_404_message": "Ne pare rău, pagina pe care o cauți nu există.",
  "error_404_backHome": "Înapoi acasă"
}
```

3. Create `src/i18n/messages/en.json` with English UI chrome translations:
```json
{
  "nav_services": "Services",
  "nav_portfolio": "Portfolio",
  "nav_pricing": "Pricing",
  "nav_about": "About",
  "nav_contact": "Contact",
  "nav_bookCall": "Book a call",
  "lang_switch_ro": "Română",
  "lang_switch_en": "English",
  "footer_copyright": "© {year} Isio. All rights reserved.",
  "footer_privacy": "Privacy Policy",
  "meta_defaultDescription": "Isio — web agency specializing in website development, web apps, SEO, and accessibility audits.",
  "pricing_basic": "Basic",
  "pricing_standard": "Standard",
  "pricing_premium": "Premium",
  "pricing_recommended": "Recommended",
  "pricing_deliveryDays": "{days} day delivery",
  "pricing_revisions": "{count} revisions",
  "pricing_revisionsUnlimited": "Unlimited revisions",
  "pricing_startingFrom": "Starting from",
  "pricing_currency": "EUR",
  "cta_getStarted": "Get started",
  "cta_contactUs": "Contact us",
  "error_404_title": "Page not found",
  "error_404_message": "Sorry, the page you are looking for does not exist.",
  "error_404_backHome": "Back to home"
}
```

4. Create `src/i18n/utils.ts` with locale utility functions. This is the central i18n utility that all pages and components import:
```typescript
export type Locale = 'ro' | 'en';

export const defaultLocale: Locale = 'ro';
export const locales: Locale[] = ['ro', 'en'];

/**
 * Slug mapping for translated URLs (per D-05).
 * Each entry maps a route key to its locale-specific slug.
 * RO slugs have no prefix. EN slugs are used under /en/.
 */
export const slugMap: Record<string, Record<Locale, string>> = {
  home:           { ro: '/',                          en: '/en/' },
  services:       { ro: '/servicii/',                 en: '/en/services/' },
  website:        { ro: '/servicii/website/',          en: '/en/services/website/' },
  webApps:        { ro: '/servicii/aplicatii-web/',    en: '/en/services/web-apps/' },
  seo:            { ro: '/servicii/seo/',              en: '/en/services/seo/' },
  accessibility:  { ro: '/servicii/accesibilitate/',   en: '/en/services/accessibility/' },
  aiAgents:       { ro: '/servicii/implementare-agenti/', en: '/en/services/ai-agents/' },
  portfolio:      { ro: '/portofoliu/',               en: '/en/portfolio/' },
  pricing:        { ro: '/pret/',                     en: '/en/pricing/' },
  about:          { ro: '/despre/',                   en: '/en/about/' },
  contact:        { ro: '/contact/',                  en: '/en/contact/' },
};

/**
 * Extract locale from a URL pathname.
 * URLs starting with /en/ are English; everything else is Romanian (default).
 */
export function getLocaleFromUrl(url: URL): Locale {
  const pathname = url.pathname;
  if (pathname.startsWith('/en/') || pathname === '/en') {
    return 'en';
  }
  return 'ro';
}

/**
 * Get the path for a given route key in the specified locale.
 * Usage: localePath('seo', 'en') => '/en/services/seo/'
 */
export function localePath(routeKey: string, locale: Locale): string {
  const entry = slugMap[routeKey];
  if (!entry) {
    console.warn(`No slug mapping found for route key: ${routeKey}`);
    return locale === 'ro' ? '/' : '/en/';
  }
  return entry[locale];
}

/**
 * Given the current URL, find the route key and return the slug for the alternate locale.
 * Used by LanguageSwitcher to link to the equivalent page in the other language.
 */
export function getAlternateLocaleSlug(url: URL): { roSlug: string; enSlug: string; routeKey: string } {
  const pathname = url.pathname;

  for (const [key, slugs] of Object.entries(slugMap)) {
    if (pathname === slugs.ro || pathname === slugs.ro.slice(0, -1)) {
      return { roSlug: slugs.ro, enSlug: slugs.en, routeKey: key };
    }
    if (pathname === slugs.en || pathname === slugs.en.slice(0, -1)) {
      return { roSlug: slugs.ro, enSlug: slugs.en, routeKey: key };
    }
  }

  // Fallback: return home slugs if no match found
  return { roSlug: '/', enSlug: '/en/', routeKey: 'home' };
}
```
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && test -f project.inlang/settings.json && test -f src/i18n/messages/ro.json && test -f src/i18n/messages/en.json && test -f src/i18n/utils.ts && echo "ALL FILES EXIST" || echo "MISSING FILES"</automated>
  </verify>
  <acceptance_criteria>
    - project.inlang/settings.json contains `"sourceLanguageTag": "ro"`
    - project.inlang/settings.json contains `"languageTags": ["ro", "en"]`
    - project.inlang/settings.json contains `pathPattern` pointing to `./src/i18n/messages/{languageTag}.json`
    - src/i18n/messages/ro.json contains `"nav_services": "Servicii"`
    - src/i18n/messages/en.json contains `"nav_services": "Services"`
    - src/i18n/messages/ro.json and en.json have identical key sets
    - src/i18n/utils.ts exports `getLocaleFromUrl` function
    - src/i18n/utils.ts exports `localePath` function
    - src/i18n/utils.ts exports `getAlternateLocaleSlug` function
    - src/i18n/utils.ts exports `slugMap` with entries for home, services, website, webApps, seo, accessibility, aiAgents, portfolio, pricing, about, contact
    - src/i18n/utils.ts contains `defaultLocale: Locale = 'ro'`
    - slugMap.home.ro equals `'/'`
    - slugMap.home.en equals `'/en/'`
    - Romanian uses comma-below diacritics (ș, ț, ă, â, î) not cedilla variants
  </acceptance_criteria>
  <done>Paraglide JS configured with project.inlang settings, bilingual UI chrome messages (RO + EN) with all navigation, pricing, CTA, and error strings, and locale utility module exporting getLocaleFromUrl, localePath, getAlternateLocaleSlug with full slug mapping for all translated URLs per D-04 and D-05.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `astro.config.mjs` has all five integrations/plugins: cloudflare, sanity, sitemap, tailwindcss, paraglide
- i18n routing: `defaultLocale: 'ro'`, `prefixDefaultLocale: false`, `locales: ['ro', 'en']`
- All locale JSON files parse without errors
- `src/i18n/utils.ts` compiles with `npx tsc --noEmit`
</verification>

<success_criteria>
- Astro 6 project builds to Cloudflare Workers target
- i18n routing configured: RO at root, EN at /en/
- Tailwind CSS v4 active via Vite plugin
- Paraglide JS compiles message catalogs to type-safe functions
- All environment variables documented in .env.example
- Zero client-side JavaScript on static pages (prerender = true pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
