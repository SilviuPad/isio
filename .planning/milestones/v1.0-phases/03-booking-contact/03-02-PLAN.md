---
phase: 03-booking-contact
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/pages/contact.astro
  - src/pages/en/contact.astro
autonomous: false
requirements: [INTR-01]
user_setup:
  - service: cal-com
    why: "Booking widget for discovery call scheduling, synced to Google Calendar"
    env_vars: []
    dashboard_config:
      - task: "Create a Cal.com account at cal.com (free tier)"
        location: "cal.com/signup"
      - task: "Create 1 event type: 'Discovery Call' — 30 minutes, set availability"
        location: "Cal.com Dashboard -> Event Types -> New Event Type"
      - task: "Connect Google Calendar for two-way sync"
        location: "Cal.com Dashboard -> Integrations -> Google Calendar -> Connect"
      - task: "Note your Cal.com username and event slug (e.g., 'your-username/discovery-call') — needed for the embed URL"
        location: "Cal.com Dashboard -> Event Types -> click the event -> copy URL slug"

must_haves:
  truths:
    - "Visitor sees Cal.com booking widget inline on the contact page where the placeholder card was"
    - "Booking widget displays in Romanian on /contact/ and English on /en/contact/"
    - "Completing a booking in the widget creates an event on the admin's Google Calendar"
    - "Both the booking embed and contact form work correctly on mobile viewport sizes"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "RO contact page with Cal.com inline embed replacing the booking placeholder card"
      contains: "Cal("
    - path: "src/pages/en/contact.astro"
      provides: "EN contact page with Cal.com inline embed"
      contains: "Cal("
  key_links:
    - from: "src/pages/contact.astro"
      to: "Cal.com hosted service"
      via: "Cal.com embed script + Cal('inline') call"
      pattern: "cal.com/embed"
    - from: "Cal.com"
      to: "Google Calendar"
      via: "Cal.com dashboard integration (configured by user, not code)"
      pattern: "N/A — external service integration"
---

<objective>
Embed the Cal.com inline booking widget into both locale contact pages, replacing the placeholder "Schedule a call" card, and verify the complete booking + contact flow works on mobile.

Purpose: Enables visitors to book discovery calls directly on-site with automatic Google Calendar sync, completing the booking conversion path (INTR-01).
Output: Working Cal.com inline embed in both locales, visual verification of full booking + contact flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-contact/03-CONTEXT.md
@.planning/phases/03-booking-contact/03-01-SUMMARY.md

@src/pages/contact.astro
@src/pages/en/contact.astro
@src/i18n/utils.ts

<interfaces>
<!-- The contact pages will have been modified by Plan 01 to include Turnstile and fetch submission.
     This plan modifies a DIFFERENT section of the same pages — the "Schedule a call" glass-card.
     The executor must read the current state of the files (post-Plan-01) before making changes. -->

Current structure in contact.astro "Schedule a call" card (RO):
```html
<div class="glass-card p-8">
  <h3 class="text-lg font-bold font-heading text-white">Programeaza un apel</h3>
  <p class="mt-3 text-base-300 text-sm leading-relaxed">
    Preferi sa discutam direct? Programeaza un apel de descoperire gratuit de 30 de minute...
  </p>
  <a href={localePath('contact', locale)} class="btn-outline inline-block mt-6 text-sm">
    Programeaza apelul
  </a>
</div>
```

Current structure in en/contact.astro "Schedule a call" card (EN):
```html
<div class="glass-card p-8">
  <h3 class="text-lg font-bold font-heading text-white">Schedule a call</h3>
  <p class="mt-3 text-base-300 text-sm leading-relaxed">
    Prefer to talk directly? Schedule a free 30-minute discovery call...
  </p>
  <a href={localePath('contact', locale)} class="btn-outline inline-block mt-6 text-sm">
    Schedule a call
  </a>
</div>
```

Cal.com embed API (vanilla JS):
```javascript
// Load script
<script src="https://app.cal.com/embed/embed.js" async></script>

// Initialize inline embed
Cal("init", { origin: "https://app.cal.com" });
Cal("inline", {
  elementOrSelector: "#cal-embed",
  calLink: "USERNAME/EVENT-SLUG",
  layout: "month_view",
  config: {
    theme: "dark",
    lang: "ro"  // or "en"
  }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace booking placeholder cards with Cal.com inline embed</name>
  <files>src/pages/contact.astro, src/pages/en/contact.astro</files>
  <read_first>
    - src/pages/contact.astro (current state after Plan 01 modifications — find the "Programeaza un apel" glass-card)
    - src/pages/en/contact.astro (current state after Plan 01 modifications — find the "Schedule a call" glass-card)
  </read_first>
  <action>
    **In both `src/pages/contact.astro` (RO) and `src/pages/en/contact.astro` (EN):**

    1. Find the first `glass-card` div in the info side column — the one with heading "Programeaza un apel" (RO) or "Schedule a call" (EN).

    2. Replace the ENTIRE glass-card div (the `<div class="glass-card p-8">` containing the heading, paragraph, and button link) with a Cal.com embed container:

       ```html
       <div class="glass-card p-8">
         <h3 class="text-lg font-bold font-heading text-white">
           <!-- RO: "Programeaza un apel de descoperire" -->
           <!-- EN: "Schedule a discovery call" -->
         </h3>
         <p class="mt-3 text-base-300 text-sm leading-relaxed mb-6">
           <!-- RO: "Alege o data si ora convenabila pentru un apel gratuit de 30 de minute." -->
           <!-- EN: "Pick a convenient date and time for a free 30-minute call." -->
         </p>
         <div id="cal-embed" style="width:100%; height:100%; overflow:auto;"></div>
       </div>
       ```

    3. Add the Cal.com embed script at the bottom of each page (after all sections, before `</Page>`):

       For **RO** page (`src/pages/contact.astro`):
       ```html
       <script is:inline src="https://app.cal.com/embed/embed.js" async></script>
       <script is:inline>
         (function() {
           function initCal() {
             if (typeof Cal === 'undefined') {
               setTimeout(initCal, 100);
               return;
             }
             Cal("init", { origin: "https://app.cal.com" });
             Cal("inline", {
               elementOrSelector: "#cal-embed",
               calLink: "YOUR_USERNAME/discovery-call",
               layout: "month_view",
               config: {
                 theme: "dark",
                 lang: "ro"
               }
             });
             Cal("ui", {
               theme: "dark",
               styles: { branding: { brandColor: "#3b82f6" } },
               hideEventTypeDetails: false
             });
           }
           if (document.readyState === 'complete') {
             initCal();
           } else {
             window.addEventListener('load', initCal);
           }
         })();
       </script>
       ```

       For **EN** page (`src/pages/en/contact.astro`):
       Same script but with `lang: "en"` instead of `lang: "ro"`.

    4. Use a placeholder Cal.com link: `calLink: "YOUR_USERNAME/discovery-call"` — The user will replace `YOUR_USERNAME` with their actual Cal.com username after creating their account. Add an HTML comment above the script: `<!-- TODO: Replace YOUR_USERNAME with your Cal.com username -->`

    5. The embed container styling: The `#cal-embed` div needs minimum height to be usable. Add inline style `min-height: 450px` to the div. The glass-card container will expand to fit.

    Per D-02: Inline embed widget via Cal.com vanilla JS snippet.
    Per D-03: Replace the existing "Schedule a call" placeholder card with the actual embed.
    Per D-04: Pass `lang` param — "ro" for Romanian page, "en" for English page.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/contact.astro contains `id="cal-embed"` div
    - src/pages/contact.astro contains `cal.com/embed/embed.js` script tag
    - src/pages/contact.astro contains `Cal("inline"` call with `lang: "ro"`
    - src/pages/contact.astro does NOT contain the old placeholder `<a` link with text "Programeaza apelul" inside the first glass-card
    - src/pages/en/contact.astro contains `id="cal-embed"` div
    - src/pages/en/contact.astro contains `Cal("inline"` call with `lang: "en"`
    - src/pages/en/contact.astro does NOT contain the old placeholder `<a` link with text "Schedule a call" inside the first glass-card (the heading can remain, the link must be replaced)
    - Both files contain `theme: "dark"` in Cal config
    - Both files contain `YOUR_USERNAME/discovery-call` as calLink placeholder
    - `astro build` completes without errors
  </acceptance_criteria>
  <done>Cal.com inline booking widget replaces the placeholder card in both locale contact pages. RO page uses lang=ro, EN page uses lang=en. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify booking and contact flows on desktop and mobile</name>
  <what-built>
    Complete Phase 3 implementation:
    - Contact form with Turnstile spam protection and Resend email delivery (Plan 01)
    - Cal.com inline booking widget in both RO and EN contact pages (Plan 02)
  </what-built>
  <how-to-verify>
    1. **Set up secrets** (if not done already):
       - Run `wrangler secret put RESEND_API_KEY` and paste your Resend API key
       - Run `wrangler secret put TURNSTILE_SECRET_KEY` and paste your Turnstile secret key
       - Add `PUBLIC_TURNSTILE_SITE_KEY` to a `.dev.vars` file for local dev: `PUBLIC_TURNSTILE_SITE_KEY=your_site_key`
       - Add `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`, `CONTACT_EMAIL` to `.dev.vars` for local dev

    2. **Start dev server**: `npm run dev`

    3. **Test contact form (RO)**: Visit `http://localhost:4321/contact/`
       - Fill out name, email, select a service, write a message
       - Click "Trimite mesajul"
       - Verify: Loading state appears on button ("Se trimite...")
       - Verify: Success message appears in green: "Mesajul a fost trimis!"
       - Verify: Check your admin inbox for the notification email

    4. **Test contact form (EN)**: Visit `http://localhost:4321/en/contact/`
       - Same test with English strings
       - Verify: Success message is in English

    5. **Test Cal.com embed (RO)**: On `/contact/`, scroll to the booking section
       - Verify: Cal.com widget loads inline (not a popup)
       - Verify: Widget UI is in Romanian
       - Verify: You can select a date and see available time slots

    6. **Test Cal.com embed (EN)**: On `/en/contact/`
       - Verify: Widget UI is in English

    7. **Mobile test**: Open browser DevTools, toggle mobile viewport (375px width)
       - Verify: Contact form stacks to single column and is fully usable
       - Verify: Cal.com embed is responsive and usable on mobile
       - Verify: Success/error messages are readable on mobile

    8. **Homepage inline form**: Visit `http://localhost:4321/` (or `/en/`)
       - Scroll to the contact section
       - Verify: Inline form has the same fetch submission behavior
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Cal.com embed script and container present in both locale pages
3. Old "Schedule a call" placeholder link replaced with embed
4. Booking widget displays in correct locale (ro/en)
5. Contact form and booking embed work on mobile viewport
</verification>

<success_criteria>
- Cal.com inline embed renders on both /contact/ and /en/contact/
- Embed displays in the correct locale (RO or EN)
- Old "Schedule a call" placeholder card is replaced
- Visual verification confirms both contact form and booking work on mobile
- End-to-end: form submission reaches admin email, booking creates calendar event
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-contact/03-02-SUMMARY.md`
</output>
