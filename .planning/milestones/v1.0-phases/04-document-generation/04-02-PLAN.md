---
phase: 04-document-generation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - admin/src/pages/dashboard.astro
  - admin/src/pages/clients/index.astro
  - admin/src/pages/clients/new.astro
  - admin/src/pages/clients/[id].astro
  - admin/src/components/Layout.astro
  - admin/src/components/Nav.astro
autonomous: true
requirements:
  - CMS-02

must_haves:
  truths:
    - "Admin can view a dashboard with deadline tracker showing all clients with project names, due dates, and status"
    - "Admin can view a list of all clients in the client directory"
    - "Admin can add a new client with all fields from D-14"
    - "Admin can edit an existing client record"
    - "Admin can delete a client from the directory"
  artifacts:
    - path: "admin/src/pages/dashboard.astro"
      provides: "Main dashboard hub with deadline tracker table"
      contains: "projectDueDate"
    - path: "admin/src/pages/clients/index.astro"
      provides: "Client directory listing page"
      contains: "/api/clients"
    - path: "admin/src/pages/clients/new.astro"
      provides: "Add new client form"
      contains: "companyName"
    - path: "admin/src/pages/clients/[id].astro"
      provides: "Edit client form with delete action"
      contains: "PUT"
    - path: "admin/src/components/Layout.astro"
      provides: "Shared admin page layout with nav"
      min_lines: 20
  key_links:
    - from: "admin/src/pages/dashboard.astro"
      to: "/api/clients"
      via: "fetch in server-side getStaticPaths or page frontmatter"
      pattern: "fetch.*api/clients"
    - from: "admin/src/pages/clients/new.astro"
      to: "/api/clients"
      via: "POST fetch on form submit"
      pattern: "fetch.*api/clients.*POST"
    - from: "admin/src/pages/clients/[id].astro"
      to: "/api/clients/"
      via: "PUT and DELETE fetch calls"
      pattern: "fetch.*api/clients/"
---

<objective>
Build the admin dashboard UI: a shared layout with navigation, the main dashboard page with deadline tracker (CMS-02), and the full client directory CRUD pages (list, add, edit, delete).

Purpose: The admin needs to manage clients and track deadlines. These pages consume the API routes from Plan 01 and will later feed client data into document generation forms (Plan 04).
Output: Functional client directory and deadline tracker dashboard, accessible after login.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-document-generation/04-CONTEXT.md
@.planning/phases/04-document-generation/04-RESEARCH.md
@.planning/phases/04-document-generation/04-01-SUMMARY.md

@admin/src/lib/schema.ts — Client table schema with all fields
@admin/src/pages/api/clients.ts — Client list + create API
@admin/src/pages/api/clients/[id].ts — Client get/update/delete API
@admin/src/middleware.ts — Auth middleware (all dashboard pages are protected)
</context>

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From admin/src/lib/schema.ts:
```typescript
export const clients = sqliteTable('clients', {
  id: text('id').primaryKey(),
  companyName: text('company_name').notNull(),
  contactPerson: text('contact_person'),
  email: text('email').notNull(),
  phone: text('phone'),
  cui: text('cui'),
  address: text('address'),
  iban: text('iban'),
  notes: text('notes'),
  projectName: text('project_name'),
  projectDueDate: text('project_due_date'),
  projectStatus: text('project_status', {
    enum: ['not_started', 'in_progress', 'review', 'blocked', 'done']
  }).default('not_started'),
  createdAt: text('created_at'),
  updatedAt: text('updated_at'),
});

export type Client = typeof clients.$inferSelect;
export type NewClient = typeof clients.$inferInsert;
```

API endpoints:
- GET /api/clients → Client[]
- POST /api/clients → Client (body: NewClient fields)
- GET /api/clients/:id → Client
- PUT /api/clients/:id → Client (body: partial fields)
- DELETE /api/clients/:id → { success: true }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create shared layout and navigation components</name>
  <files>
    admin/src/components/Layout.astro,
    admin/src/components/Nav.astro
  </files>
  <read_first>
    admin/src/pages/index.astro,
    admin/src/styles/global.css,
    admin/astro.config.mjs
  </read_first>
  <action>
    Create a shared admin layout and navigation component (per D-04 clean utilitarian UI). These are NOT the public site's dark theme — use a light, minimal aesthetic.

    1. **admin/src/components/Nav.astro** — Top navigation bar:
       - Horizontal nav bar: bg-white border-b border-gray-200 px-6 py-3
       - Left: "Isio Admin" text (font-bold text-gray-900)
       - Center links: Dashboard (/dashboard), Clients (/clients), Generate Document (/generate) — each as `<a>` with text-gray-600 hover:text-gray-900 font-medium. Highlight active link with text-blue-600 using Astro.url.pathname comparison.
       - Right: Logout button — text-gray-500 hover:text-red-600, onclick calls fetch('/api/auth', { method: 'POST', body: JSON.stringify({ action: 'logout' }) }) then redirects to /

       Accept a `currentPath` prop (string) to determine active nav item.

    2. **admin/src/components/Layout.astro** — Shared page wrapper:
       ```astro
       ---
       interface Props {
         title: string;
       }
       const { title } = Astro.props;
       const currentPath = Astro.url.pathname;
       ---
       <!DOCTYPE html>
       <html lang="en">
       <head>
         <meta charset="utf-8" />
         <meta name="viewport" content="width=device-width, initial-scale=1" />
         <meta name="robots" content="noindex, nofollow" />
         <title>{title} — Isio Admin</title>
         <link rel="stylesheet" href="/styles/global.css" />
       </head>
       <body class="bg-gray-50 text-gray-900 min-h-screen">
         <Nav currentPath={currentPath} />
         <main class="max-w-6xl mx-auto px-6 py-8">
           <slot />
         </main>
       </body>
       </html>
       ```
       The `<meta name="robots" content="noindex, nofollow" />` ensures admin pages are never indexed by search engines.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - admin/src/components/Layout.astro contains "noindex, nofollow" and "Isio Admin" and "<Nav"
    - admin/src/components/Layout.astro has Props interface with "title: string"
    - admin/src/components/Nav.astro contains "/dashboard" and "/clients" and "/generate" as nav links
    - admin/src/components/Nav.astro contains "action: 'logout'" fetch call to /api/auth
    - admin/src/components/Nav.astro contains "currentPath" prop usage for active link highlighting
  </acceptance_criteria>
  <done>Shared Layout and Nav components exist. Layout wraps all admin pages with nav, prevents search engine indexing. Nav shows Dashboard, Clients, Generate links with active state and logout button.</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard with deadline tracker and client directory pages</name>
  <files>
    admin/src/pages/dashboard.astro,
    admin/src/pages/clients/index.astro,
    admin/src/pages/clients/new.astro,
    admin/src/pages/clients/[id].astro
  </files>
  <read_first>
    admin/src/components/Layout.astro,
    admin/src/components/Nav.astro,
    admin/src/lib/schema.ts,
    admin/src/pages/api/clients.ts,
    admin/src/pages/api/clients/[id].ts
  </read_first>
  <action>
    Build 4 pages that implement the client directory and deadline tracker. All pages use SSR (export const prerender = false). All pages use the Layout component. Per D-04, use clean utilitarian styling with Tailwind.

    1. **admin/src/pages/dashboard.astro** — Main hub with deadline tracker (CMS-02):
       - Page title: "Dashboard"
       - Fetch clients from API using Astro server-side: `const response = await fetch(new URL('/api/clients', Astro.url)); const allClients = await response.json();`
       - **Deadline Tracker section** (per D-16, D-17, CMS-02):
         - Heading: "Deadline Tracker" (text-lg font-semibold)
         - Filter to only clients with projectName set (skip clients with no project)
         - Sort by projectDueDate ascending (soonest first, nulls last)
         - HTML table with columns: Client (companyName), Project (projectName), Due Date (projectDueDate formatted), Status (projectStatus)
         - Status rendered as colored badge:
           - not_started: bg-gray-100 text-gray-700
           - in_progress: bg-blue-100 text-blue-700
           - review: bg-yellow-100 text-yellow-700
           - blocked: bg-red-100 text-red-700
           - done: bg-green-100 text-green-700
         - Display status label with spaces: "Not started", "In progress", "Review", "Blocked", "Done"
         - Overdue dates (before today) shown with text-red-600 font-medium
         - If no clients with projects: show "No active projects" message
       - **Quick Stats row** above tracker: Total clients count, Active projects count (status !== done), Overdue count
       - Link to /clients for full directory, link to /generate for document generation

    2. **admin/src/pages/clients/index.astro** — Client directory listing:
       - Page title: "Clients"
       - Fetch all clients from API server-side (same pattern as dashboard)
       - "Add Client" button linking to /clients/new (bg-blue-600 text-white rounded px-4 py-2)
       - Table with columns: Company/Name (companyName), Contact (contactPerson), Email, Phone, Project, Status, Actions
       - Actions column: Edit link (/clients/{id}), Delete button
       - Delete button uses is:inline script with fetch DELETE to /api/clients/{id}, then window.location.reload()
       - If no clients: show "No clients yet. Add your first client." with link to /clients/new
       - Confirm dialog before delete: `if (!confirm('Delete this client?')) return;`

    3. **admin/src/pages/clients/new.astro** — Add new client form:
       - Page title: "Add Client"
       - Form with all fields from D-14:
         - Company/Person Name (text, required) — name="companyName"
         - Contact Person (text) — name="contactPerson"
         - Email (email, required) — name="email"
         - Phone (tel) — name="phone"
         - CUI / Fiscal Code (text) — name="cui"
         - Address (textarea) — name="address"
         - IBAN / Bank Details (text) — name="iban"
         - Notes (textarea) — name="notes"
       - Project section:
         - Project Name (text) — name="projectName"
         - Due Date (date input) — name="projectDueDate"
         - Status (select dropdown with 5 options from D-17) — name="projectStatus"
       - Form styling: labels as text-sm font-medium text-gray-700, inputs with border border-gray-300 rounded-md px-3 py-2 w-full, two-column grid on large screens (grid grid-cols-1 md:grid-cols-2 gap-4)
       - Submit button: "Save Client" (bg-blue-600 text-white rounded px-4 py-2 hover:bg-blue-700)
       - Cancel link back to /clients
       - is:inline script: prevent default, collect form data as JSON, POST to /api/clients, on 201 redirect to /clients, on error show inline error message

    4. **admin/src/pages/clients/[id].astro** — Edit client form + delete:
       - Page title: "Edit Client"
       - SSR page: extract id from Astro.params, fetch client from /api/clients/{id} server-side
       - If 404: show "Client not found" with link back to /clients
       - Same form layout as new.astro but pre-filled with existing client data using `value={client.fieldName}` on each input
       - Two action buttons:
         - "Save Changes" (bg-blue-600) — PUT to /api/clients/{id}
         - "Delete Client" (bg-red-600 text-white) — DELETE to /api/clients/{id} with confirm dialog, redirect to /clients on success
       - is:inline script: handles both save (PUT) and delete (DELETE) fetch calls
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio/admin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - admin/src/pages/dashboard.astro contains "export const prerender = false" and "Deadline Tracker" and "projectDueDate" and "projectStatus"
    - admin/src/pages/dashboard.astro contains status badge classes "bg-blue-100" and "bg-red-100" and "bg-green-100"
    - admin/src/pages/dashboard.astro contains "No active projects" empty state message
    - admin/src/pages/clients/index.astro contains "/api/clients" and "Add Client" and "/clients/new"
    - admin/src/pages/clients/index.astro contains "confirm('Delete this client?')" delete confirmation
    - admin/src/pages/clients/new.astro contains all D-14 field names: "companyName" and "contactPerson" and "email" and "phone" and "cui" and "address" and "iban" and "notes"
    - admin/src/pages/clients/new.astro contains project fields: "projectName" and "projectDueDate" and "projectStatus"
    - admin/src/pages/clients/new.astro contains select dropdown with "not_started" and "in_progress" and "review" and "blocked" and "done"
    - admin/src/pages/clients/[id].astro contains "PUT" and "DELETE" fetch calls
    - admin/src/pages/clients/[id].astro contains "Client not found" 404 handling
    - All 4 pages import and use the Layout component
  </acceptance_criteria>
  <done>Dashboard displays deadline tracker (CMS-02) with client names, project names, due dates, and color-coded status badges. Client directory allows full CRUD: list all clients, add new clients with all D-14 fields, edit existing clients, delete with confirmation.</done>
</task>

</tasks>

<verification>
1. `cd admin && npx tsc --noEmit` completes with zero errors
2. All 6 files in files_modified exist
3. Dashboard page displays deadline tracker with correct status badges
4. Client CRUD pages handle all form fields from D-14, D-15, D-16, D-17
</verification>

<success_criteria>
- Dashboard shows deadline tracker table with client name, project name, due date, and color-coded status per CMS-02
- Client directory listing page shows all clients with edit/delete actions
- New client form has all 14 fields from D-14 plus project fields from D-15/D-16/D-17
- Edit client form pre-fills with existing data and supports update + delete
- All pages use shared Layout component with navigation
- Admin can navigate between Dashboard, Clients, and (future) Generate Document
</success_criteria>

<output>
After completion, create `.planning/phases/04-document-generation/04-02-SUMMARY.md`
</output>
