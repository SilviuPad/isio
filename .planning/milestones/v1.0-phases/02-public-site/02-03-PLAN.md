---
phase: 02-public-site
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/servicii/website.astro
  - src/pages/servicii/aplicatii-web.astro
  - src/pages/servicii/seo.astro
  - src/pages/servicii/accesibilitate.astro
  - src/pages/servicii/implementare-agenti.astro
  - src/pages/en/services/website.astro
  - src/pages/en/services/web-apps.astro
  - src/pages/en/services/seo.astro
  - src/pages/en/services/accessibility.astro
  - src/pages/en/services/ai-agents.astro
  - src/components/services/ServicePageLayout.astro
  - src/components/services/ServiceFeatureList.astro
  - src/components/services/ServiceCTA.astro
  - src/lib/service-data.ts
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05

must_haves:
  truths:
    - "Each of the 5 services has a dedicated page in both RO and EN (10 pages total)"
    - "Each service page shows scope, deliverables, and turnaround per SITE-01 through SITE-05"
    - "Each service page ends with dual CTAs: View pricing + Book a call (per D-07)"
    - "Service pages use a shared template with per-service variations (per D-05)"
  artifacts:
    - path: "src/components/services/ServicePageLayout.astro"
      provides: "Shared service page template"
      min_lines: 30
    - path: "src/pages/servicii/website.astro"
      provides: "RO website builds service page"
      contains: "prerender"
    - path: "src/pages/en/services/seo.astro"
      provides: "EN SEO service page"
      contains: "prerender"
    - path: "src/lib/service-data.ts"
      provides: "Extended service content with scope, deliverables, turnaround"
      contains: "export"
  key_links:
    - from: "src/pages/servicii/website.astro"
      to: "src/components/services/ServicePageLayout.astro"
      via: "Astro component import"
      pattern: "import ServicePageLayout"
    - from: "src/components/services/ServiceCTA.astro"
      to: "src/i18n/utils.ts"
      via: "localePath for pricing and contact links"
      pattern: "localePath"
    - from: "src/lib/service-data.ts"
      to: "src/lib/content.ts"
      via: "extends service content with detail sections"
      pattern: "serviceKey"
---

<objective>
Build all 5 service detail pages in both locales (10 pages total) using a shared template component with per-service content variations. Each page shows scope, deliverables, turnaround, and dual CTAs.

Purpose: Service pages are the core conversion pages. Each service (website builds, web apps, SEO, accessibility, AI agents) needs its own page that communicates value, scope, and deliverables clearly enough for a prospect to understand what they're buying.

Output: 10 page files (5 RO + 5 EN), 3 service layout components, 1 service data module.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-site/02-CONTEXT.md

@src/layouts/Page.astro
@src/lib/content.ts
@src/i18n/utils.ts
@src/content/services/web-development.json
@src/content/services/web-apps.json
@src/content/services/seo.json
@src/content/services/accessibility.json
@src/styles/global.css

<interfaces>
From src/layouts/Page.astro:
```typescript
interface Props {
  title: string;
  description?: string;
  locale: Locale;
  roSlug: string;
  enSlug: string;
  ogImage?: string;
}
```

From src/lib/content.ts:
```typescript
export async function getServiceByKey(serviceKey: string): Promise<{serviceKey, slug, title, description, metaTitle, metaDescription, order} | null>
export function localize(field: LocaleString, locale: Locale): string
```

From src/i18n/utils.ts:
```typescript
export const slugMap = {
  website:       { ro: '/servicii/website/',             en: '/en/services/website/' },
  webApps:       { ro: '/servicii/aplicatii-web/',       en: '/en/services/web-apps/' },
  seo:           { ro: '/servicii/seo/',                 en: '/en/services/seo/' },
  accessibility: { ro: '/servicii/accesibilitate/',      en: '/en/services/accessibility/' },
  aiAgents:      { ro: '/servicii/implementare-agenti/', en: '/en/services/ai-agents/' },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service data module and shared service page components</name>
  <files>
    src/lib/service-data.ts
    src/components/services/ServicePageLayout.astro
    src/components/services/ServiceFeatureList.astro
    src/components/services/ServiceCTA.astro
  </files>
  <read_first>
    - src/lib/content.ts (getServiceByKey, localize signatures)
    - src/content/services/web-development.json (service JSON structure)
    - src/content/services/seo.json (service JSON structure)
    - src/content/services/accessibility.json (service JSON structure)
    - src/content/services/web-apps.json (service JSON structure)
    - src/i18n/utils.ts (localePath, Locale type, slugMap)
    - src/layouts/Page.astro (Props interface)
    - src/styles/global.css (design system tokens)
    - .planning/phases/02-public-site/02-CONTEXT.md (D-05, D-06, D-07 decisions)
  </read_first>
  <action>
**src/lib/service-data.ts** -- Extended service content that is too detailed for JSON files. Contains scope sections, deliverables lists, turnaround info, and per-service unique sections. Each service has a `sections` array with bilingual content.

```typescript
import type { Locale } from '../i18n/utils';

interface LocaleString { ro: string; en: string; }

interface ServiceSection {
  id: string;
  title: LocaleString;
  items: Array<{ text: LocaleString; icon?: string }>;
}

interface ServiceDetail {
  serviceKey: string;
  tagline: LocaleString;
  turnaroundDays: LocaleString;
  sections: ServiceSection[];
}

export const serviceDetails: Record<string, ServiceDetail> = {
  'web-development': {
    serviceKey: 'web-development',
    tagline: {
      ro: 'Website-uri rapide, accesibile și optimizate care transformă vizitatorii în clienți.',
      en: 'Fast, accessible, optimized websites that turn visitors into customers.',
    },
    turnaroundDays: { ro: '14-45 zile', en: '14-45 days' },
    sections: [
      {
        id: 'scope',
        title: { ro: 'Ce include', en: "What's included" },
        items: [
          { text: { ro: 'Design responsive mobile-first', en: 'Mobile-first responsive design' } },
          { text: { ro: 'Optimizare SEO on-page', en: 'On-page SEO optimization' } },
          { text: { ro: 'Integrare CMS (Sanity, WordPress)', en: 'CMS integration (Sanity, WordPress)' } },
          { text: { ro: 'Formular de contact funcțional', en: 'Working contact form' } },
          { text: { ro: 'Hosting și deployment pe Cloudflare', en: 'Cloudflare hosting and deployment' } },
          { text: { ro: 'Certificat SSL și securitate de bază', en: 'SSL certificate and basic security' } },
        ],
      },
      {
        id: 'deliverables',
        title: { ro: 'Ce primești', en: 'What you get' },
        items: [
          { text: { ro: 'Cod sursă complet cu documentație', en: 'Complete source code with documentation' } },
          { text: { ro: 'Scor Lighthouse 90+ pe toate categoriile', en: 'Lighthouse score 90+ on all categories' } },
          { text: { ro: 'Training pentru editarea conținutului CMS', en: 'CMS content editing training' } },
          { text: { ro: '30 de zile suport post-lansare', en: '30 days post-launch support' } },
        ],
      },
    ],
  },
  'web-apps': {
    serviceKey: 'web-apps',
    tagline: {
      ro: 'Aplicații web robuste cu funcționalități complexe, construite pentru scalare.',
      en: 'Robust web applications with complex features, built to scale.',
    },
    turnaroundDays: { ro: '30-60 zile', en: '30-60 days' },
    sections: [
      {
        id: 'scope',
        title: { ro: 'Ce include', en: "What's included" },
        items: [
          { text: { ro: 'Arhitectură full-stack cu TypeScript', en: 'Full-stack TypeScript architecture' } },
          { text: { ro: 'Autentificare și autorizare utilizatori', en: 'User authentication and authorization' } },
          { text: { ro: 'Bază de date PostgreSQL optimizată', en: 'Optimized PostgreSQL database' } },
          { text: { ro: 'API REST sau GraphQL', en: 'REST or GraphQL API' } },
          { text: { ro: 'Dashboard admin personalizat', en: 'Custom admin dashboard' } },
          { text: { ro: 'Integrări third-party (plăți, email, etc.)', en: 'Third-party integrations (payments, email, etc.)' } },
        ],
      },
      {
        id: 'deliverables',
        title: { ro: 'Ce primești', en: 'What you get' },
        items: [
          { text: { ro: 'Aplicație funcțională deployată', en: 'Deployed functional application' } },
          { text: { ro: 'Documentație API completă', en: 'Complete API documentation' } },
          { text: { ro: 'Pipeline CI/CD configurată', en: 'Configured CI/CD pipeline' } },
          { text: { ro: '60 de zile suport post-lansare', en: '60 days post-launch support' } },
        ],
      },
    ],
  },
  'seo': {
    serviceKey: 'seo',
    tagline: {
      ro: 'Strategie SEO bazată pe date care aduce trafic organic și conversii.',
      en: 'Data-driven SEO strategy that drives organic traffic and conversions.',
    },
    turnaroundDays: { ro: '14-30 zile', en: '14-30 days' },
    sections: [
      {
        id: 'scope',
        title: { ro: 'Ce include', en: "What's included" },
        items: [
          { text: { ro: 'Audit SEO tehnic complet (140+ verificări)', en: 'Complete technical SEO audit (140+ checks)' } },
          { text: { ro: 'Cercetare cuvinte cheie și analiza competitorilor', en: 'Keyword research and competitor analysis' } },
          { text: { ro: 'Optimizare on-page (meta tags, structură, conținut)', en: 'On-page optimization (meta tags, structure, content)' } },
          { text: { ro: 'Optimizare Schema.org și date structurate', en: 'Schema.org and structured data optimization' } },
          { text: { ro: 'Monitorizare poziții și raportare', en: 'Position monitoring and reporting' } },
          { text: { ro: 'Strategie de link building', en: 'Link building strategy' } },
        ],
      },
      {
        id: 'deliverables',
        title: { ro: 'Ce primești', en: 'What you get' },
        items: [
          { text: { ro: 'Raport de audit detaliat cu priorități', en: 'Detailed audit report with priorities' } },
          { text: { ro: 'Plan de acțiune SEO pe 3 luni', en: '3-month SEO action plan' } },
          { text: { ro: 'Dashboard cu metrici și progres', en: 'Metrics and progress dashboard' } },
          { text: { ro: 'Rapoarte lunare de performanță', en: 'Monthly performance reports' } },
        ],
      },
    ],
  },
  'accessibility': {
    serviceKey: 'accessibility',
    tagline: {
      ro: 'Audituri de accesibilitate WCAG care fac site-ul tău utilizabil de toți.',
      en: 'WCAG accessibility audits that make your site usable by everyone.',
    },
    turnaroundDays: { ro: '10-21 zile', en: '10-21 days' },
    sections: [
      {
        id: 'scope',
        title: { ro: 'Ce include', en: "What's included" },
        items: [
          { text: { ro: 'Audit WCAG 2.1 (nivel A, AA sau AAA)', en: 'WCAG 2.1 audit (Level A, AA, or AAA)' } },
          { text: { ro: 'Testare cu screen reader (NVDA, VoiceOver)', en: 'Screen reader testing (NVDA, VoiceOver)' } },
          { text: { ro: 'Testare navigare cu tastatură', en: 'Keyboard navigation testing' } },
          { text: { ro: 'Verificare contrast culori și tipografie', en: 'Color contrast and typography check' } },
          { text: { ro: 'Testare formulare și elemente interactive', en: 'Form and interactive element testing' } },
          { text: { ro: 'Verificare compatibilitate dispozitive asistive', en: 'Assistive device compatibility check' } },
        ],
      },
      {
        id: 'deliverables',
        title: { ro: 'Ce primești', en: 'What you get' },
        items: [
          { text: { ro: 'Raport de audit cu capturile de ecran', en: 'Audit report with screenshots' } },
          { text: { ro: 'Lista de probleme prioritizată pe severitate', en: 'Issue list prioritized by severity' } },
          { text: { ro: 'Ghid de remediere pas cu pas', en: 'Step-by-step remediation guide' } },
          { text: { ro: 'Re-testare după remediere (pachetele Standard+)', en: 'Re-test after remediation (Standard+ plans)' } },
        ],
      },
    ],
  },
  'ai-agents': {
    serviceKey: 'ai-agents',
    tagline: {
      ro: 'Agenți AI autonomi care lucrează 24/7 pentru afacerea ta.',
      en: 'Autonomous AI agents that work 24/7 for your business.',
    },
    turnaroundDays: { ro: '21-60 zile', en: '21-60 days' },
    sections: [
      {
        id: 'scope',
        title: { ro: 'Ce include', en: "What's included" },
        items: [
          { text: { ro: 'Analiză procese și identificare oportunități de automatizare', en: 'Process analysis and automation opportunity identification' } },
          { text: { ro: 'Proiectare arhitectură agent (single sau multi-agent)', en: 'Agent architecture design (single or multi-agent)' } },
          { text: { ro: 'Integrare cu API-uri existente și baze de date', en: 'Integration with existing APIs and databases' } },
          { text: { ro: 'Monitorizare și alertare automată', en: 'Automated monitoring and alerting' } },
          { text: { ro: 'Dashboard de control și raportare', en: 'Control dashboard and reporting' } },
          { text: { ro: 'Deployment pe VPS sau cloud', en: 'VPS or cloud deployment' } },
        ],
      },
      {
        id: 'deliverables',
        title: { ro: 'Ce primești', en: 'What you get' },
        items: [
          { text: { ro: 'Agent funcțional deployat în producție', en: 'Functional agent deployed to production' } },
          { text: { ro: 'Documentație completă a arhitecturii', en: 'Complete architecture documentation' } },
          { text: { ro: 'Training operare și monitorizare', en: 'Operation and monitoring training' } },
          { text: { ro: '90 de zile suport și optimizare post-lansare', en: '90 days post-launch support and optimization' } },
        ],
      },
    ],
  },
};

export function getServiceDetail(serviceKey: string): ServiceDetail | undefined {
  return serviceDetails[serviceKey];
}
```

**src/components/services/ServicePageLayout.astro:**
Props: `{ serviceKey: string; locale: Locale }`.
This is the shared template (per D-05). In frontmatter:
- Import `getServiceByKey`, `localize` from `../../lib/content`
- Import `getServiceDetail` from `../../lib/service-data`
- Fetch the CMS service data and the extended detail data
- Layout: Hero section with `bg-navy-950 py-20`, service title in `text-4xl font-bold font-heading text-white`, tagline in `text-xl text-navy-300`. Below hero: section blocks iterating over `detail.sections`, each with title in `text-2xl font-bold font-heading text-navy-950` and an unordered list with items. Turnaround shown in a dedicated block with `bg-navy-50 rounded-xl p-8` and the turnaround days highlighted in `text-electric-500 font-bold`. After all sections: `<ServiceCTA locale={locale} />`.

**src/components/services/ServiceFeatureList.astro:**
Props: `{ items: Array<{ text: { ro: string; en: string } }>; locale: Locale }`.
Renders a `<ul>` with `space-y-4`. Each item has a checkmark SVG icon (inline SVG, `text-electric-500 w-5 h-5`) + text in `text-navy-700`.

**src/components/services/ServiceCTA.astro:**
Props: `{ locale: Locale }`.
Per D-07: dual CTA at end of each service page.
Section with `bg-navy-50 rounded-2xl p-12 mt-16 text-center`. Two buttons:
- "View pricing" linking to `localePath('pricing', locale)` -- `bg-electric-500 hover:bg-electric-600 text-white px-8 py-4 rounded-full font-semibold`
- "Book a call" linking to `localePath('contact', locale)` -- `border-2 border-navy-300 text-navy-700 hover:border-electric-500 hover:text-electric-500 px-8 py-4 rounded-full font-semibold`
Bilingual labels using inline Record pattern.
Per D-06: pricing lives on separate /pricing page only -- service pages do NOT embed pricing tiers.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/service-data.ts exists and contains `export const serviceDetails`
    - src/lib/service-data.ts contains all 5 serviceKeys: `web-development`, `web-apps`, `seo`, `accessibility`, `ai-agents`
    - src/lib/service-data.ts contains `export function getServiceDetail`
    - src/components/services/ServicePageLayout.astro exists and contains `getServiceDetail`
    - src/components/services/ServicePageLayout.astro contains `bg-navy-950`
    - src/components/services/ServiceFeatureList.astro exists and contains `text-electric-500`
    - src/components/services/ServiceCTA.astro exists and contains `localePath('pricing'`
    - src/components/services/ServiceCTA.astro contains `localePath('contact'`
    - Astro build succeeds
  </acceptance_criteria>
  <done>Shared service page layout, feature list component, dual CTA component, and extended service data module are created. All 5 services have scope, deliverables, and turnaround content.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 10 service detail pages (5 RO + 5 EN)</name>
  <files>
    src/pages/servicii/website.astro
    src/pages/servicii/aplicatii-web.astro
    src/pages/servicii/seo.astro
    src/pages/servicii/accesibilitate.astro
    src/pages/servicii/implementare-agenti.astro
    src/pages/en/services/website.astro
    src/pages/en/services/web-apps.astro
    src/pages/en/services/seo.astro
    src/pages/en/services/accessibility.astro
    src/pages/en/services/ai-agents.astro
  </files>
  <read_first>
    - src/pages/index.astro (reference for page file pattern: prerender, imports, slug setup)
    - src/pages/pret.astro (reference for existing page pattern)
    - src/i18n/utils.ts (slugMap -- exact slugs for each service)
    - src/components/services/ServicePageLayout.astro (just created -- Props interface)
    - src/lib/content.ts (getServiceByKey, localize)
    - src/lib/service-data.ts (getServiceDetail)
  </read_first>
  <action>
Create 10 page files. Each follows the identical pattern:

```astro
---
export const prerender = true;

import Page from '../../layouts/Page.astro';
import ServicePageLayout from '../../components/services/ServicePageLayout.astro';
import { getServiceByKey, localize } from '../../lib/content';
import { slugMap } from '../../i18n/utils';
import type { Locale } from '../../i18n/utils';

const locale: Locale = 'ro'; // or 'en' for EN pages
const serviceKey = 'web-development'; // varies per page
const slugMapKey = 'website'; // varies per page
const { roSlug, enSlug } = { roSlug: slugMap[slugMapKey].ro, enSlug: slugMap[slugMapKey].en };

const service = await getServiceByKey(serviceKey);
const title = service ? localize(service.metaTitle, locale) : 'Service — Isio';
const description = service ? localize(service.metaDescription, locale) : '';
---
<Page title={title} description={description} locale={locale} roSlug={roSlug} enSlug={enSlug}>
  <ServicePageLayout serviceKey={serviceKey} locale={locale} />
</Page>
```

**Mapping for all 10 pages:**

| File | locale | serviceKey | slugMapKey |
|------|--------|------------|------------|
| servicii/website.astro | ro | web-development | website |
| servicii/aplicatii-web.astro | ro | web-apps | webApps |
| servicii/seo.astro | ro | seo | seo |
| servicii/accesibilitate.astro | ro | accessibility | accessibility |
| servicii/implementare-agenti.astro | ro | ai-agents | aiAgents |
| en/services/website.astro | en | web-development | website |
| en/services/web-apps.astro | en | web-apps | webApps |
| en/services/seo.astro | en | seo | seo |
| en/services/accessibility.astro | en | accessibility | accessibility |
| en/services/ai-agents.astro | en | ai-agents | aiAgents |

The import path depth changes for RO pages (../../) vs EN pages (../../../) because EN pages are one directory deeper.
  </action>
  <verify>
    <automated>cd C:/Users/sylvi/Documents/projects/isio && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/servicii/website.astro exists and contains `serviceKey = 'web-development'`
    - src/pages/servicii/aplicatii-web.astro exists and contains `serviceKey = 'web-apps'`
    - src/pages/servicii/seo.astro exists and contains `serviceKey = 'seo'`
    - src/pages/servicii/accesibilitate.astro exists and contains `serviceKey = 'accessibility'`
    - src/pages/servicii/implementare-agenti.astro exists and contains `serviceKey = 'ai-agents'`
    - src/pages/en/services/website.astro exists and contains `locale: Locale = 'en'`
    - src/pages/en/services/web-apps.astro exists and contains `locale: Locale = 'en'`
    - src/pages/en/services/seo.astro exists and contains `locale: Locale = 'en'`
    - src/pages/en/services/accessibility.astro exists and contains `locale: Locale = 'en'`
    - src/pages/en/services/ai-agents.astro exists and contains `locale: Locale = 'en'`
    - All 10 files contain `export const prerender = true`
    - All 10 files contain `ServicePageLayout`
    - `npx astro build` produces output for all 10 service routes
  </acceptance_criteria>
  <done>All 5 service detail pages exist in both RO and EN, using the shared template with per-service content. Each shows scope, deliverables, turnaround, and dual CTAs.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds
- All 10 service page routes exist in the build output
- Each service page renders scope and deliverables sections
- Each service page ends with "View pricing" + "Book a call" CTAs
</verification>

<success_criteria>
- 10 service detail pages (5 RO + 5 EN) render correctly
- All pages use the shared ServicePageLayout template
- Each service has unique scope, deliverables, and turnaround content
- Dual CTAs link to /pricing and /contact respectively per locale
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-site/02-03-SUMMARY.md`
</output>
